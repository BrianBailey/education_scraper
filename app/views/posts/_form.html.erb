<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :speaker_state %><br>
    <%= f.text_field :speaker_state %>
  </div>
  <div class="field">
    <%= f.label :speaker_first %><br>
    <%= f.text_field :speaker_first %>
  </div>
  <div class="field">
    <%= f.label :speaker_last %><br>
    <%= f.text_field :speaker_last %>
  </div>
  <div class="field">
    <%= f.label :speaker_party %><br>
    <%= f.text_field :speaker_party %>
  </div>
  <div class="field">
    <%= f.label :speaking %><br>
    <%= f.text_area :speaking %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
